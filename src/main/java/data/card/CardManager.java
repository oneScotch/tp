package data.card;

import java.util.ArrayList;


\
    public CardManager(List<Card> cards) {
        this.cards = cards;
    }

    pat! You collect card: " + card.toString());
    }

    /**
     * find card that is not collected within a certain range.
     * @return]\artIndex; i < startIndex + NUM_OF_CARDS_PER_GAME; i++) {
            if (!cards.get(i).checkIfCollected()) {
                index = i;
            }
        }
        return index;
    }


    /**\s.size();
        int index = -1;
          if (cards.get(i).getCardID() == cardID) {
     index = i;
                break;
            }
        }t = 0;
        for (int i = 0; i < len; i++) {
            Card currentCard = cards.get(i);
            if (curreent().contains(message)) {
                Syst
    public void listCards() {
        int len = cards.size();
        for (int i = 0; i < len; i++) {
            Card card = cards.get(i);
            d.checkIfIsUsed() ? MARK_USED : MARK_NOT_USED;
            iprintln(e.getMessage());
            return null;
        }\
        return canBeExchanged;
    }
}